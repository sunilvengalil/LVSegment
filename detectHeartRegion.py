import pdb
from matplotlib import pyplot

#Function to detect the heart region in all the given images
def detect_heart_region(images):
    (num_slices, num_times, width,height) = images.shape

    #pyplot.imshow(images[0,0,:,:]);
    #pyplot.show();
    

    

    
    pdb.set_trace()

    return 1
